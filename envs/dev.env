# =============================================================================
# NOTIFICATION ORCHESTRATOR - Development Environment
# =============================================================================

# Application
MIX_ENV=dev
PORT=4004
PHX_HOST=localhost
PHX_SERVER=true

# Security
SECRET_KEY_BASE=dev_secret_key_base_min_64_chars_long_for_phoenix_app_1234567890abcdef
JWT_SECRET=dev_jwt_secret_change_in_production

# MongoDB
MONGODB_URL=mongodb://admin:admin_secret@localhost:27017/notification_db?authSource=admin

# Redis
REDIS_URL=redis://:redis_secret@localhost:6379

# Kafka
KAFKA_BROKERS=localhost:9092

# Auth Service
AUTH_SERVICE_URL=http://localhost:8081

# Firebase Cloud Messaging (FCM)
FCM_PROJECT_ID=
FCM_PRIVATE_KEY=
FCM_CLIENT_EMAIL=

# Apple Push Notification Service (APNS)
APNS_KEY_ID=
APNS_TEAM_ID=
APNS_KEY_PATH=
APNS_ENV=sandbox

# Email (SMTP)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@localhost
SMTP_FROM_NAME=QuckApp

# Erlang Distribution
RELEASE_COOKIE=quckapp_dev_cookie
NODE_NAME=notification@localhost

# Observability
JAEGER_ENDPOINT=http://localhost:14268/api/traces
LOG_LEVEL=debug

# Feature Flags
ENABLE_METRICS=true
ENABLE_TRACING=true
ENABLE_EMAIL=false
ENABLE_PUSH=false
